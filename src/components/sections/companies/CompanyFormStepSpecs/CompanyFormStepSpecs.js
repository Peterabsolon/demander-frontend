import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class CompanyFormStepSpecs extends Component {

  static propTypes = {
    something: PropTypes.any
  }

  render() {
    return (
      <div>CompanyFormStepSpecs</div>
    )
  }
}
