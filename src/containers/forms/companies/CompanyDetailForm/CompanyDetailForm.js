import React, { Component } from 'react'
import PropTypes from 'prop-types'

export default class CompanyDetailForm extends Component {
  static propTypes = {
    something: PropTypes.any
  };

  render() {
    return <div>CompanyDetailForm</div>
  }
}
